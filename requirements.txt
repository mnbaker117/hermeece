# Production runtime dependencies for Hermeece.
# Test deps live in requirements-dev.txt.

# Web framework
fastapi==0.121.0
uvicorn[standard]==0.30.0

# Async DB
aiosqlite==0.20.0

# HTTP (MAM cookie validation, .torrent grab, qBittorrent API)
httpx==0.27.0

# Background scheduling (Phase 3: cookie checks, weekly audit, daily digest)
apscheduler==3.10.4

# Data validation (FastAPI request/response models)
pydantic==2.8.0

# Auth scaffolding (Phase 3 — versions matched to AthenaScout)
bcrypt==4.2.1
itsdangerous==2.2.0

# Embedded ebook metadata reading (Phase 2)
ebooklib==0.18

# Embedded audiobook metadata reading (Phase 2)
mutagen==1.47.0
